/// A module containing models fetched from the ch.tetr.io API
/// Almost all models supported are "equivalent" to the ones shown in the API documentations.
pub mod cache;
/// A module containing models fetched from the ch.tetr.io API
/// Almost all models supported are "equivalent" to the ones shown in the API documentations.
pub mod packet;
/// A module containing models fetched from the ch.tetr.io API
/// Almost all models supported are "equivalent" to the ones shown in the API documentations.
pub mod users;
/// A module containing models fetched from the ch.tetr.io API
/// Almost all models supported are "equivalent" to the ones shown in the API documentations.
pub mod streams;
/// A module containing models fetched from the ch.tetr.io API
/// Almost all models supported are "equivalent" to the ones shown in the API documentations.
pub mod news;
/// A module containing models fetched from the ch.tetr.io API
/// Almost all models supported are "equivalent" to the ones shown in the API documentations.
pub mod general;

pub mod common;

pub mod labs;