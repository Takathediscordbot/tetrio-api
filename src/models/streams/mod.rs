//! [Stream](https://tetr.io/about/api/#streamsstream) models
//! As the documentation for those models is not available, the fields are not documented.
//! You will have to test the routes yourself to figure out which fields you can get.

/// Stream models
/// As the documentation for those models is not available, the fields are not documented.
/// You will have to test the routes yourself to figure out which fields you can get.
pub mod stream;
/// League stream models
/// As the documentation for those models is not available, the fields are not documented.
/// These modules are still trying to be accurate and they are tested but I can not provide documentation for each of them.
pub mod league_stream;
